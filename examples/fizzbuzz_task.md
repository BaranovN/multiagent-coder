# FizzBuzz

Read a single integer `N` from stdin (1 ≤ N ≤ 1_000_000).

For every integer `i` from 1 to `N` inclusive, print on its own line:

- `FizzBuzz` if `i` is divisible by both 3 and 5
- `Fizz` if `i` is divisible by 3 only
- `Buzz` if `i` is divisible by 5 only
- `i` otherwise
